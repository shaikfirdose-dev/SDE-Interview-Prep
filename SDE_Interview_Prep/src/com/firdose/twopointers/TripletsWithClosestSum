Question:

Given an array of unsorted numbers and a target number, find a triplet in the array whose sum is as close to the target number as possible, return the sum of the triplet. If there are more than one such triplet, return the sum of the triplet with the smallest sum.

Input: [-3, -1, 1, 2], target=1
Output: 0

T.C:O(N*2)
S.C:O(1)
---------------------------------


import java.util.*;

class Solution {

  public int searchTriplet(int[] arr, int target) {
    int n = arr.length;
    int diff = Integer.MAX_VALUE;

    Arrays.sort(arr);

    for(int i=0;i<n-2;i++){
      int left = i+1;
      int right = n-1;

      while(left < right){
        int targetSum = arr[i]+arr[left]+arr[right];

        int targetDiff = target-targetSum;

        if(targetDiff==0){
          return targetSum;
        }

        if(Math.abs(targetDiff) < Math.abs(diff) || (Math.abs(targetDiff)==Math.abs(diff) && targetDiff>diff) ){
          diff = targetDiff;
        }
        if(targetDiff > 0){
          left++;
        }
        else{
          right--;
        }
      }
    }

    return target-diff;
  }
}
